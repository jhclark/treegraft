#include <sent_vec.h>

typedef struct _suff_arrT {
	
} suff_arrT;

suff_arrT* suffix_array_build(sent_vecT* sentences) {
	// STEP 1: Create a stable set of C data structures
}

void suffix_array_sort(suff_arrT* arr) {
	
}

void suffix_array_search(suff_arrT* arr) {
	
}